import React from "react";
import { SafeAreaView, Text } from "react-native";

const TimeOverview = () => {
    return(
        <SafeAreaView>
            <Text style = {{ fontSize: 20}}>WIP</Text>
        </SafeAreaView>
    )
}
export default (TimeOverview)